apiVersion: tmr.tempo/v1alpha1
kind: TraceQueries
queries:
  - id: checkout-http-legacy
    name: Checkout requests using the legacy HTTP attribute
    criticality: critical
    expression: '{ span.http.method = "GET" }'
